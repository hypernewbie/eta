{
  "name": "eta-web",
  "private": true,
  "scripts": {
    "build:web": "tsc --project tsconfig.json",
    "check:web": "tsc --noEmit --project tsconfig.json",
    "format:web": "prettier --write web/app.ts web/style.css web/index.html",
    "test:web": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "prettier": "3.5.3",
    "typescript": "5.7.3"
  }
}
